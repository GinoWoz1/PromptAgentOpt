[project]
name = "promptagentopt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "files-to-prompt>=0.6",
    "langfuse>=3.3.2",
    "pydantic>=2.11.7",
    "rich>=14.1.0",
]
